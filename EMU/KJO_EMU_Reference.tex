\documentclass[11pt,letterpaper]{article}
\input{../shared/KJO_preamble}
\newcommand{\docversion}{0.3}
\newcommand{\docdate}{March 2026}
\lhead{\textsc{KJO Engine Management Unit}}
\rhead{\textsc{EMU Reference} --- v\docversion}
\cfoot{\thepage\ of \pageref{LastPage}}
\hypersetup{pdftitle={KJO EMU Reference}, pdfauthor={Ken Overton}}

\begin{document}
\input{../shared/KJO_tikz_styles}

\KJOtitlepage%
  {Engine Management Unit (EMU)}%
  {Hardware and Software Reference}%
  {\docversion}{\docdate}%
  {This document describes the hardware, software architecture, valve control
   system, Fill valve handshake protocol, sensor subsystems, and control flow
   of the Engine Management Unit.  The EMU is mounted inside the rocket and
   is the primary actuator and sensor node of the KJO system.}

\tableofcontents\newpage

%═══════════════════════════════════════════════════════════════════════════════
\section{Hardware Overview}
%═══════════════════════════════════════════════════════════════════════════════

\subsection{Component Summary}

\begin{center}
\begin{tabular}{@{}llll@{}}
\toprule
Component & Part & Interface & Notes \\
\midrule
Microcontroller   & Adafruit Feather M0 (ATSAMD21G18A, 48~MHz)  & ---     & \\
OLED Display      & SH1107, 128$\times$64, 1.12\textquotedbl      & I\textsuperscript{2}C & \\
GPIO Expander     & MCP23X17 (16 I/O)                            & I\textsuperscript{2}C & Relays, buttons, AUX\_IO \\
PWM Servo Wing    & Adafruit 8-channel wing (PCA9685)            & I\textsuperscript{2}C & Ox, Fuel, Dump servos \\
ADC               & ADS1015 (12-bit, 4-ch)                       & I\textsuperscript{2}C & Pressure transducers \\
Thermocouple IF   & MAX31856 (2$\times$)                         & SPI     & TC1, TC2 \\
Real-Time Clock   & PCF8523                                      & I\textsuperscript{2}C & \\
LoRa Radio        & HopeRF RFM95W (Semtech SX1276), 915~MHz      & SPI     & \\
SD Card           & Standard SPI SD socket                       & SPI     & \\
NeoPixel          & WS2812B (1$\times$)                          & GPIO    & Status indicator \\
Igniter Relay     & SPDT relay via MCP23X17                      & GPIO    & \\
Buzzer            & Passive piezo                                & PWM     & \\
Battery           & 3.7~V LiPo; voltage divider on ADC           & ADC     & \\
\bottomrule
\end{tabular}
\end{center}

\subsection{MCP23X17 GPIO Expander Pin Map}

\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
Pin Name & Direction & Function \\
\midrule
Relay outputs (4$\times$) & OUT & Igniter relay and auxiliary relays \\
Button inputs (4$\times$) & IN  & Local panel buttons \\
\sig{AUX\_IO\_1}     & OUT & Fill valve CMD line (to GSEMU) \\
\sig{AUX\_IO\_2}     & IN  & Fill valve STATUS line (from GSEMU) \\
\sig{AUX\_IO\_3} (\sig{QR\_CMD\_EIO})         & OUT & Umbilical QR release CMD (HIGH = latch hold/armed, LOW = release) \\
\sig{AUX\_IO\_4} (\sig{RELEASE\_STATE\_EIO})  & IN  & Umbilical separation state (INPUT\_PULLUP; LOW = connected, HIGH = separated) \\
\sig{AUX\_IO\_5} (\sig{LAUNCH\_ENABLE\_EIO})  & OUT & Remote start enable to GSEMU (HIGH = disabled default, LOW = enabled) \\
\sig{AUX\_IO\_6} (\sig{REMOTE\_START\_EIO})   & IN  & Remote start command from GSEMU (INPUT\_PULLUP; LOW = start commanded) \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Hardware Block Diagram}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.9cm and 1.8cm]
  \node[mcublock, minimum width=3.0cm, minimum height=1.1cm] (mcu)
    {Feather M0\\(ATSAMD21G18A)};
  % I2C left
  \node[hwblock, left=2.3cm of mcu, yshift= 3.2cm, minimum width=3.2cm] (oled) {SH1107 OLED};
  \node[hwblock, left=2.3cm of mcu, yshift= 2.0cm, minimum width=3.2cm] (gpio) {MCP23X17\\GPIO Exp.};
  \node[hwblock, left=2.3cm of mcu, yshift= 0.8cm, minimum width=3.2cm] (pwm)  {PCA9685\\Servo Wing};
  \node[hwblock, left=2.3cm of mcu, yshift=-0.4cm, minimum width=3.2cm] (adc)  {ADS1015 ADC};
  \node[hwblock, left=2.3cm of mcu, yshift=-1.6cm, minimum width=3.2cm] (rtc)  {PCF8523 RTC};
  % SPI right
  \node[hwblock, right=2.3cm of mcu, yshift= 2.0cm, minimum width=3.0cm] (rf)  {RFM95W LoRa};
  \node[hwblock, right=2.3cm of mcu, yshift= 0.8cm, minimum width=3.0cm] (tc1) {MAX31856 TC×2};
  \node[hwblock, right=2.3cm of mcu, yshift=-0.4cm, minimum width=3.0cm] (sd)  {SD Card};
  % GPIO below
  \node[hwblock, below=1.5cm of mcu, xshift=-1.8cm, minimum width=2.8cm] (neo) {NeoPixel};
  \node[hwblock, below=1.5cm of mcu, xshift= 1.8cm, minimum width=2.8cm] (buz) {Buzzer};
  % Power
  \node[powerblock, below=3.0cm of mcu, minimum width=3.0cm] (bat) {LiPo Battery};
  % I2C bus (left)
  \foreach \nd in {oled,gpio,pwm,adc,rtc}
    \draw[bidbusline] (mcu.west |- \nd) -- (\nd.east)
      node[midway,above,font=\tiny]{I\textsuperscript{2}C};
  % SPI bus (right)
  \foreach \nd in {rf,tc1,sd}
    \draw[bidbusline] (mcu.east |- \nd) -- (\nd.west)
      node[midway,above,font=\tiny]{SPI};
  % GPIO
  \draw[busline] (mcu.south -| neo) -- (neo.north);
  \draw[busline] (mcu.south -| buz) -- (buz.north);
  % Power
  \draw[busline,draw=KJOorange] (bat.north) -- ++(0,0.6) -| (mcu.south)
    node[near end,right,font=\tiny]{3.7V};
\end{tikzpicture}
\caption{EMU hardware block diagram.}
\end{figure}

%═══════════════════════════════════════════════════════════════════════════════
\section{Software Architecture}
%═══════════════════════════════════════════════════════════════════════════════

\subsection{Module Summary}

\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
Module & Location & Responsibility \\
\midrule
\sw{main.cpp}              & local  & Setup, loop, command dispatch \\
\sw{KJO\_Valve}            & local  & Base valve: servo drive, pot feedback \\
\sw{KJO\_Master\_Valve}    & local  & Fill valve CMD/STATUS GPIO handshake \\
\sw{KJO\_QR\_Master}       & local  & Umbilical QR release proxy: level-based CMD (\sig{AUX\_IO\_3}), separation state (\sig{AUX\_IO\_4}) \\
\sw{KJO\_Control\_System}  & local  & \sw{Execute\_Command()} routing table \\
\sw{KJO\_Pressure}         & local  & Pressure transducer read / convert \\
\sw{KJO\_Temperature}      & local  & Analog temperature transducer \\
\sw{KJO\_Thermocouple}     & local  & MAX31856 thermocouple read \\
\sw{KJO\_Analog}           & local  & ADS1015 ADC wrapper \\
\sw{KJO\_GPIO}             & local  & MCP23X17 pin definitions, Relay class \\
\sw{KJO\_EMU\_Display}     & local  & SH1107 OLED display functions \\
\sw{KJO\_Indicator}        & local  & NeoPixel status indicator \\
\sw{KJO\_Logging}          & local  & SD log file constants \\
\sw{KJO\_Radio}            & shared & Radio pin constants, frequency \\
\sw{KJO\_Command\_Defs}    & shared & Command codes, packet structures \\
\sw{KJO\_System\_Config}   & shared & Version, platform constants \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Main Loop Control Flow}

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=0.55cm]
  \node[flowterminal] (start)  {loop()};
  \node[flowproc, below=of start]  (btn)  {Check\_Buttons()};
  \node[flowproc, below=of btn]    (vchk) {Check\_Valve\_Move()};
  \node[flowproc, below=of vchk]   (rx)   {Check\_Radio()};
  \draw[flowarrow] (start)--(btn);
  \draw[flowarrow] (btn)--(vchk);
  \draw[flowarrow] (vchk)--(rx);
  \draw[flowarrow] (rx.south) -- ++(0,-0.4) -- ++(3.5,0)
    -- ++(0,4.5) -- (start.east);
\end{tikzpicture}
\caption{EMU main loop control flow.}
\end{figure}

%═══════════════════════════════════════════════════════════════════════════════
\section{Valve Control Subsystem}
%═══════════════════════════════════════════════════════════════════════════════

\subsection{Valve Base Class (\sw{KJO\_Valve})}

The \sw{Valve} class encapsulates a servo-driven, potentiometer-feedback valve:

\begin{itemize}[nosep]
  \item \textbf{Drive}: PWM signal to PCA9685 servo wing channel.
  \item \textbf{Feedback}: Potentiometer on ADS1015 ADC channel; position
        expressed as 0--100\% open.
  \item \textbf{Position control}: \sw{openTo(\%)} sets target; \sw{update()}
        drives servo until pot reading reaches target within dead-band.
  \item \textbf{State queries}: \sw{isMoving()}, \sw{isStopped()},
        \sw{isOpen()}, \sw{isClosed()}.
\end{itemize}

\subsection{Deferred Valve State Machine}

Valve movements are not blocking.  \sw{Check\_Valve\_Move()} is called every
loop iteration and monitors the active valve:

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=2.5cm]
  \node[statenode] (idle)   {IDLE};
  \node[statenode, right=of idle] (moving) {MOVING};
  \draw[statetransition, bend left=20]
    (idle) edge node[above, font=\tiny, align=center]
      {valve command\\received}
    (moving);
  \draw[statetransition, bend left=20]
    (moving) edge node[below, font=\tiny, align=center]
      {pos. reached\\or timeout}
    (idle);
  \draw[statetransition, loop above, looseness=5]
    (moving) edge node[above, font=\tiny] {still moving} (moving);
\end{tikzpicture}
\caption{Deferred valve state machine.}
\end{figure}

A watchdog timer prevents the system from remaining in MOVING indefinitely
if the servo stalls or the potentiometer fails.

\subsection{Master\_Valve Class (\sw{KJO\_Master\_Valve})}

\sw{Master\_Valve} extends \sw{Valve} with the CMD/STATUS GPIO handshake for
the Fill valve.  On \sw{open()} or \sw{close()}, the class asserts
\sig{AUX\_IO\_1} (CMD) and then polls \sig{AUX\_IO\_2} (STATUS) to confirm that
the GSEMU has completed the commanded movement.  The GSEMU drives the servo;
the EMU's \sw{Valve} base class is not used for Fill valve servo drive.

%═══════════════════════════════════════════════════════════════════════════════
\section{Fill Valve Handshake Protocol}
%═══════════════════════════════════════════════════════════════════════════════

The Fill valve servo and potentiometer reside on the GSEMU.
The EMU and GSEMU exchange state using two GPIO lines:

\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
Signal & Driver & Meaning when HIGH \\
\midrule
\sig{AUX\_IO\_1} (CMD)    & EMU   & Fill valve commanded open \\
\sig{AUX\_IO\_2} (STATUS) & GSEMU & Fill valve has reached commanded position \\
\bottomrule
\end{tabular}
\end{center}

\textbf{Open sequence:}
\begin{enumerate}[nosep]
  \item EMU calls \sw{Fill\_Valve.open()}.
  \item \sw{Master\_Valve} asserts CMD = HIGH.
  \item GSEMU \sw{Slave\_Valve.update()} detects CMD rising edge; drives servo to open.
  \item GSEMU asserts STATUS = HIGH when servo reaches open position.
  \item EMU reads STATUS = HIGH; reports completion.
\end{enumerate}

\textbf{Close sequence}: CMD de-asserted (LOW); GSEMU closes servo; STATUS driven LOW.

%═══════════════════════════════════════════════════════════════════════════════
\section{Command Dispatch (\sw{Execute\_Command})}
%═══════════════════════════════════════════════════════════════════════════════

\sw{Check\_Radio()} receives and parses packets from the RCU.
\cmd{HEARTBEAT} is handled directly and a \texttt{Heartbeat\_Response\_Packet}
is returned immediately, containing EMU voltage and current servo positions.
All other commands are dispatched through \sw{Execute\_Command(command\_ID, param)},
which implements a \texttt{switch} on command code.  Commands with deferred
completion (e.g., \cmd{DUMP}, \cmd{BEGIN\_FILL}) put the valve state machine
into MOVING state; the acknowledgement is sent when the movement completes in
\sw{Check\_Valve\_Move()}.

\cmd{QR\_RELEASE} (code~25) is dispatched through \sw{Execute\_Command()} to
\sw{QR\_Master::release()}, which drives \sig{AUX\_IO\_3} (\sig{QR\_CMD\_EIO})
LOW to command the GSEMU servo to open the umbilical latch.  The CMD line
remains LOW until \sw{hold()} is called or the system is reset.  The response
is returned immediately; physical separation is confirmed asynchronously by
polling \sw{QR\_Master::isSeparated()}, which reads \sig{AUX\_IO\_4}
(\sig{RELEASE\_STATE\_EIO}) --- that line goes HIGH when the umbilical connector
physically disconnects (INPUT\_PULLUP floats HIGH).

%═══════════════════════════════════════════════════════════════════════════════
\section{Umbilical Quick-Release (\sw{QR\_Master})}
%═══════════════════════════════════════════════════════════════════════════════

\subsection{Overview}

\sw{QR\_Master} is the EMU-side proxy for the umbilical quick-release actuator
that resides on the GSEMU.  It drives two MCP23X17 expansion I/O lines:

\begin{itemize}[nosep]
  \item \sig{AUX\_IO\_3} (\sig{QR\_CMD\_EIO}) --- OUTPUT.  Level-based command
        to the GSEMU QR servo.
  \item \sig{AUX\_IO\_4} (\sig{RELEASE\_STATE\_EIO}) --- INPUT\_PULLUP.  Wired
        to GSEMU chassis GND through the AUX connector.  Reads LOW while the
        umbilical is physically connected; floats HIGH when the connector is
        removed (separation confirmed).
\end{itemize}

\subsection{Signal Protocol}

\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
\sig{QR\_CMD\_EIO} Level & Driver & Meaning \\
\midrule
HIGH (default) & EMU & Latch engaged --- GSEMU servo held at hold position \\
LOW            & EMU & Latch open --- GSEMU servo commanded to release position \\
\bottomrule
\end{tabular}
\end{center}

\sw{QR\_Master::begin()} configures \sig{QR\_CMD\_EIO} as OUTPUT and drives it
HIGH on startup, so the GSEMU latch remains engaged until an explicit
\cmd{QR\_RELEASE} command or \sw{release()} call is received.
The safe default is therefore \emph{latch closed}: a disconnected or
unpowered AUX connector cannot accidentally trigger a release.

\subsection{Separation Detection}

\sig{RELEASE\_STATE\_EIO} is configured as INPUT\_PULLUP and is wired to GSEMU
GND through the umbilical connector.  When the umbilical is connected the line
reads LOW.  When the connector is physically removed the INPUT\_PULLUP pulls
the line HIGH.  \sw{QR\_Master::update()} (or any call to
\sw{isSeparated()}) latches this transition; once latched it remains true for
the remainder of the session.

\subsection{QR\_Master API}

\begin{center}
\begin{tabular}{@{}ll@{}}
\toprule
Method & Action \\
\midrule
\sw{begin()}       & Configures CMD pin as OUTPUT (HIGH), state pin as INPUT\_PULLUP \\
\sw{release()}     & Drives \sig{QR\_CMD\_EIO} LOW --- commands GSEMU to open latch \\
\sw{hold()}        & Drives \sig{QR\_CMD\_EIO} HIGH --- commands GSEMU to hold latch \\
\sw{isConnected()} & Returns true while \sig{RELEASE\_STATE\_EIO} reads LOW \\
\sw{isSeparated()} & Returns true (latched) once state line has gone HIGH \\
\bottomrule
\end{tabular}
\end{center}

\begin{warningbox}
Ensure the launch sequence has been verified and all personnel are clear
before issuing \cmd{QR\_RELEASE}.  The umbilical disconnection releases
pressurised N\textsubscript{2}O.  Calling \sw{hold()} after \sw{release()}
returns the CMD line HIGH but cannot re-engage a latch that has already
opened mechanically.
\end{warningbox}

%═══════════════════════════════════════════════════════════════════════════════
\section{Sensor Subsystems}
%═══════════════════════════════════════════════════════════════════════════════

\begin{center}
\begin{tabular}{@{}llll@{}}
\toprule
Sensor & Class & ADC / Bus & Measurement \\
\midrule
Tank pressure transducer   & \sw{Pressure\_Transducer}   & ADS1015 ch0 & N\textsubscript{2}O tank pressure \\
Chamber pressure transducer& \sw{Pressure\_Transducer}   & ADS1015 ch1 & Combustion chamber pressure \\
Tank temperature sensor    & \sw{Temperature\_Transducer}& ADS1015 ch2 & N\textsubscript{2}O tank temperature \\
Chamber temperature sensor & \sw{Temperature\_Transducer}& ADS1015 ch3 & Chamber wall temperature \\
Thermocouple 1             & \sw{MAX31856}               & SPI         & TC1 (TBD location) \\
Thermocouple 2             & \sw{MAX31856}               & SPI         & TC2 (TBD location) \\
\bottomrule
\end{tabular}
\end{center}

All sensor reads are non-blocking; results are returned in
\texttt{Command\_Return\_Packet.r\_val} in response to the corresponding
\cmd{READ\_*} commands.

%═══════════════════════════════════════════════════════════════════════════════
\section{SD Logging}
%═══════════════════════════════════════════════════════════════════════════════

Log files are stored in \texttt{EMU\_Logs/Log\_N.txt}.  Each record is
time-stamped (PCF8523) and records command receipt, valve movements, sensor
readings, and fault events.

%═══════════════════════════════════════════════════════════════════════════════
\section{Version History}
%═══════════════════════════════════════════════════════════════════════════════
\begin{center}
\begin{tabular}{@{}lll@{}}
\toprule
Version & Date & Notes \\
\midrule
0.1 & March 2026 & Initial release. \\
0.2 & March 2026 & Add umbilical QR release subsystem (QR\_Master, QR\_RELEASE command, AUX\_IO\_3 protocol). \\
0.3 & March 2026 & Level-based QR protocol (release=LOW, hold=HIGH); add RELEASE\_STATE\_EIO (AUX\_IO\_4),
                   LAUNCH\_ENABLE\_EIO (AUX\_IO\_5), REMOTE\_START\_EIO (AUX\_IO\_6);
                   Button~A reassigned to hold-to-open Dump valve;
                   \sw{QR\_Master} API updated: \sw{release()}, \sw{hold()}, \sw{isSeparated()}, \sw{isConnected()}. \\
\bottomrule
\end{tabular}
\end{center}

\end{document}
